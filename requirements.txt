# PT-Accelerator Python 运行依赖

# Web 框架与服务启动
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9

# 网络请求与 DNS 解析
httpx>=0.27.0
dnspython>=2.6.1

# 下载器客户端 SDK
qbittorrent-api>=2026.5.1
transmission-rpc>=4.3.0

# 配置文件与数据校验
pyyaml>=6.0.1
pydantic>=2.6.0

# 定时任务调度
apscheduler>=3.10.4

# 密码学：AES-128-GCM 加密（密码/凭据存储必需）
cryptography>=42.0.0

# 前端本地构建环境（提示用，非 pip 依赖）
# node>=18.0.0
# npm>=9.0.0
